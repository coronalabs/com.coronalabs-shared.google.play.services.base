Sources for the plugin `shared.google.play.services.base`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "shared.google.play.services.base" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
